动态数组让我们可以根据需要灵活地申请内存，而不必一开始就定死大小。这在处理不确定数量的数据时非常有用。
我们之前学会了申请一个 的空间，代码是 malloc(sizeof(int))
int *arr = malloc(5 * sizeof(int));
这行代码就在堆上开辟了连续的 20 个字节（假设 是 4 字节），并把首地址给了 。
虽然我们之前强调过要检查 ，但在 C 语言中，最神奇的地方在于：你可以像使用普通数组一样使用指针。if (arr != NULL)
