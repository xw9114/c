// 1. 申请：要一个 int 的空间
int *p = malloc(sizeof(int));

if (p != NULL) {
    // 2. 使用：存入数据
    *p = 10;
    
    // 3. 释放：归还给系统
    free(p);
    
    // 4. 销毁：避免误用（防止悬空指针）
    p = NULL;
}

// 5. 检查：再次使用前确认
if (p != NULL) {
    printf("%d", *p); // 这一行现在绝对安全，不会执行，也不会报错
}
